<% if defined?(image) && image %>
  <%= image_tag image.attachment.url(:product), itemprop: 'image', 'data-large': image.attachment.url(:large) %>
<% else %>
  <%= product_image @product, itemprop: 'image' %>
<% end %>
